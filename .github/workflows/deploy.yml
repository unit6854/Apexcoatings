name: Deploy to Namecheap Staging

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Deployment triggered
      run: |
        echo "✅ Deployment triggered successfully at $(date)"